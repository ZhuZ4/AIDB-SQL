mini-dev 300 题持续实验计划

日期：2026-09-21。本文保留原始实验设计；自动实施已启动，当前执行状态和可用命令见 `experiments/STATUS.md` 与 `experiments/README.md`。下文“待实现”描述的是制定计划时的起点，不代表当前工作区状态。

目标：形成固定 300 题的 question → 最终 SQL → 官方执行评分结果，并通过“评估 → 错误归因 → 查论文 → 改代码 → 重评估”的循环寻找可复现提升。300 题指 300 个完整处理记录，不能承诺 300 题全部答对；失败、超时和未提交 SQL 也必须保留。

**1. 已核实的起点**

| 项目 | 本地事实 / 后续影响 |
|---|---|
| 项目 | `F:/data/VSCodeproject/AIDB-SQL`，当前分支 `codex/dev_legion`，HEAD `bd8502f`，工作区有未提交的部署和检索改动 |
| 远端 | `origin = https://github.com/ZhuZ4/AIDB-SQL.git`；尚未验证本轮推送认证 |
| 业务数据 | `F:/data/VSCodeproject/minidev/MINIDEV/dev_databases`；已只读枚举 11 库、75 表、798 列 |
| 题目 | `mini_dev_sqlite.json`：500 个唯一 question_id，含 question、evidence、SQL、db_id、difficulty |
| 向量 | 上次入库核验记录为 20 个字段、37 个向量；双向量 RRF 已实现，BM25 未实现；正式评测前须重新检查服务并建立全量索引 |
| 字段说明 | 既有审计为 699 列有说明、99 列说明缺失；全量入库时再次按原始 SQLite 列名核对，缺说明不等于缺字段 |
| Agent | `agent.py` 提供 `AgentService.run_query`；`tools/native_sql_tools.py` 提供最终 SQL 和执行轨迹；数据库连接、当前会话含模块全局状态 |
| 缺失技能 | 当前只有 data-link、database-query-helper、sql-error-analysis；主流程引用的 correct、schema-exploration 尚缺。先补齐实际调用契约；report-generation 非本轮评分前置条件 |
| 模型配置 | 用户已指定本项目 `.env` 和 `deepseek-v4.1-flash`。文件原有 API_KEY / BASE_URL 已映射到项目读取的 LITE_LLM_API_KEY / LITE_LLM_BASE_URL，并设置 LITE_LLM_MODEL_NAME；没有输出密钥或发送模型请求，连通性和工具调用能力仍待冒烟验证 |
| 批量入口 | 当前项目没有可用的 test_bird_dev.py / evaluate.py，需新增运行器和官方评价器适配层 |
| 官方评价器 | `F:/data/VSCodeproject/mini_dev/evaluation` 已有 EX / F1 / VES 脚本；该仓库 HEAD 为 `abd11b6db92a1c9f809b32f7564c7c71b34d67f0`，本次检查工作区干净 |
| Codex CLI | 已发现本地命令，版本 `0.154.0`；未启动后台实验调度 |

题目 JSON 的 SHA256：`def4b2b43a9b06955193418f24c9be170eb6d83763ab702311790e0cdda8c791`。每次运行必须校验该哈希，版本变化后新建实验系列，不与旧结果直接比较。

原始 `mini_dev_sqlite_gold.sql` 的静态检查发现：第 33、499、500 行缺少制表符分隔；第 485、486、487 行的 SQL 与同序 JSON 不一致（比较时已忽略空白与末尾分号）。这只证明格式/文本不一致，尚未证明答案语义不同。不要直接把原 gold 文件交给当前评分器，也不要修改原始文件。以本轮锁定的题目 JSON 为单一来源，导出对应的 gold 输入，记录上述差异与来源。

**2. 固定实验集与评测口径**

已生成 `experiments/mini_dev_300_manifest.json`，仅包含 ID、抽样方法、来源哈希和分布，不含标准答案。

- 从 500 题按 `(db_id, difficulty)` 分层，用比例配额及最大余数法选出 300 题；层内按固定种子与 question_id 的 SHA256 排序。输出顺序沿用原始 JSON。
- 固定种子：`aidb-minidev-300-v1-20260921`；包含 simple 89、moderate 150、challenging 61 题，覆盖全部 11 个数据库。
- 每轮使用同一批 300 题、同一题目顺序与 SQLite 数据。另从其中固定约 30 题作快速回归，覆盖 11 库和三种难度，不随错误样本临时替换。
- 这 300 题是允许观察错误、反复迭代的开发集。保留的 200 题不用于改提示词或挑方法；后续若需要证明泛化，再对冻结的方法做一次独立验证。当前交付不强制增加到 500 题。
- Agent 输入仅为 question、evidence、对应数据库及可检索元数据。所有轮次固定使用 evidence。SQL 标准答案只进入隔离的评估/开发集错误分析环节，不进入生成输入或检索语料。
- 每题以 `submitted_final_sql` / `get_final_sql(session_id)` 为准，保存工具原始提交文本。`run_query` 的 generated_sql 可能回退到启发式 SQL，且会处理 LIMIT，不能直接作为本轮评分唯一依据。
- 主指标 EX = 官方执行判对题数 / 300。语法正确率、提交率、超时率、分库/分难度 EX、延迟和 token 是辅助指标。F1 作诊断；官方 VES 在正确性稳定后复测，避免 GPU/CPU 同时负载污染性能结果。
- 当前本地官方 EX 使用结果元组集合相等，忽略顺序与重复次数。保持此口径以便比较；必要时增加顺序/多重集诊断，但不得将自定义诊断冒充官方 EX。

**3. 先补齐实验基础，再建立基线 B0**

依次执行，阶段完成条件明确记录在状态文件中：

| 阶段 | 工作 | 完成条件 |
|---|---|---|
| P0 数据与评分校准 | 按 manifest 导出同序 question/evidence、gold.sql、难度 JSONL；核对 300 个 ID、数据库和文件哈希 | 生成端看不到 gold；输入长度一致；gold 对 gold 自检通过；缺预测/故意错误 SQL 均判 0 |
| P1 全量双向量索引 | 以 SQLite 枚举的全部 798 列入库，说明取 JSON + CSV，缺说明保留 NULL；列名/说明保持独立嵌入 | 11 库全覆盖、列身份一致、索引可复现、无跨库召回；保存索引版本与内容哈希 |
| P2 恢复完整主流程 | 补 correct / schema-exploration、逐题只读 SQLite 切库、显式最终 SQL 提交、独立会话、有限纠错；启动服务并检查模型接口 | 约 30 题能产生逐题终态和官方评分；中断恢复后没有重号、漏题、跨库状态 |
| P3 B0 基线 | 固定模型、提示、工具、纠错预算和完整双向量索引；暂不加 BM25 | 300 个处理记录、300 个评分、分组指标、失败分类、Git commit 和运行清单完整 |
| P4 单变量改进 | 优先由 B0 错误决定候选；每轮一个主要方法，用配置开关控制 | 快速回归通过后完成同样 300 题，与当前最佳版本逐题对照 |
| P5 留存/继续 | 改进满足采用规则则更新最佳版本；否则保留失败实验并继续定位 | 状态落盘、代码与指标对应、下一步明确，无需用户逐轮催促 |

P2 的冒烟用于验证工程链路，不把 20 列样例下的分数当成 B0；正式冒烟与 B0 均须在 P1 全量索引完成后运行。基线必要修复必须在 B0 前完成并冻结，不能将“补齐索引或缺失技能”混算成 BM25 的收益。

评分适配层复用锁定版本的官方评分逻辑，补充只读连接、逐题诊断、Windows 多进程入口和统一导出。原题 JSON 是数组，官方难度读取器需要 JSONL；预测 JSON 以插入顺序配对，gold 每行要求 `SQL<TAB>db_id`，预测格式要求 `SQL<TAB>----- bird -----<TAB>db_id`。导出器必须验证原始 ID 到连续评分序号的映射，以及路径末尾分隔符。

官方入口会 zip 预测与 gold，长度不足可能漏评，因此必须先断言三份输入均为 300 且顺序一致。失败题保存空的真实 final_sql，并用经验证必定报错的专用占位 SQL 导出评分，不能使用空查询或 `SELECT 1`，以免意外与空/常量答案相等。超时、API 失败和无提交都保留在分母中。

**4. 每一轮怎样寻找问题和方法**

每轮执行状态流：

```text
PREFLIGHT → GENERATE → EVALUATE → DIAGNOSE → RESEARCH
          → IMPLEMENT → SMOKE → COMPARE → KEEP / REJECT → DIAGNOSE ...
```

“没有提升”不会直接结束任务：保存实验、回到归因，选择新的可验证假设。即使有提升，也继续分析剩余的主要错误，直至达到本轮完成条件或真实资源边界。

| 错误类别 | 先查证据 | 确认后再查的方法方向 |
|---|---|---|
| 召回漏表/漏列 | 各路候选、名次、最终 M-Schema、缺失字段 | BM25 + 双向量 RRF、标识符拆分、重排、缺说明列的召回策略 |
| 值匹配或 evidence 使用错误 | 查询条件、实际值、证据是否进入提示 | 值检索、词面/语义匹配、证据约束 |
| JOIN 路径错误 | 主外键、连接列、重复扩增 | 外键图扩展、连接路径选择、最小连接子图 |
| 聚合/粒度/排序错误 | 初稿与最终 SQL、执行差异 | 查询分解、约束检查、针对聚合和排序的校验 |
| 执行成功但语义错误 | 与 gold 结果差异、问题要求 | 执行反馈、候选比较、语义验证 |
| 循环/工具或服务失败 | 调用轨迹、异常、限流、耗时 | 工程修复与预算控制，不能直接归因于检索算法 |

论文研究必须在明确错误类别后进行。每轮记录 1–3 篇原始论文及官方实现，精读相关方法/消融，写明“论文解决什么问题 → 本项目哪类错误符合前提 → 最小可实施改动 → 预期改变哪个指标 → 额外成本”。不能根据摘要直接假定有提升。

候选第一组可以是“完整双向量 B0 → 加 BM25 → 加精确标识符保留 → 加重排”，但只有前一轮证据支持时才进入下一项。关键词检索应独立召回并与向量合并，不能作为向量检索的硬过滤。真正 BM25 使用 pg_search 等实现；原生 ts_rank 另标实验名称，不混称 BM25。

凡新增多候选生成、重排或更大模型，必须记入 token、调用次数和延迟，并单独标注成本变化。不得把模型升级、检索变更和提示词重写同时混入一个主实验。禁止按 question_id 硬编码答案、把开发集 gold 注入提示词、改评分规则或删掉难题。

**5. 怎样判定提升并采用代码**

- 同一 300 题逐题配对，输出“原错变对数 / 原对变错数 / 净增正确数 / ΔEX”，同时分库和分难度汇总。
- 300 题每多答对 1 题约增加 0.333 个百分点。建议主要里程碑设为较 B0 净增至少 6 题（+2 个百分点）；这是工程验收建议，不是预先保证，也不等同于统计显著。
- 候选更新最佳版本至少要有正向净收益、工程检查通过、成本在设定上限内；小幅收益标为暂定，不能把单次波动直接写成稳定提升。
- 对准备采用的候选与 B0/当前最佳版本做匹配的重复运行，比较均值与逐题变化；固定温度/种子（服务支持时）仍不保证完全确定性。重复次数和预算在启动时冻结，不挑最好的一次汇报。
- 保存配对 bootstrap 置信区间或 McNemar 检验作为辅助证据；重复搜索很多方法后的开发集显著性不能替代独立验证。
- 候选没有提升或回退：保留分支、配置与错误分析，最佳版本不变；更新下一轮假设。连续 3 个不同假设无效时，自动重新审视错误分类和实验设计，避免反复微调同一参数。
- 最终保留的 question–SQL 文件来自同一个冻结版本和固定运行规则，不能逐题拼接各实验最好 SQL 后称为单次结果。

**6. 长期运行和恢复机制**

区分两层：Codex 负责读日志、找论文、改代码和管理实验；项目 SQL Agent 负责按固定接口答题。SQL Agent 的 API 配置与 Codex 自身登录/额度是不同条件。

优先使用本任务的 Goal 模式持续推进，配合独立的本地实验运行器持久化进度。若需要跨进程重启后自动继续，可由本地监督进程调用 Codex CLI 非交互任务，并使用明确 session ID 恢复，或启动新会话读取磁盘状态；不要依赖可能指向其他任务的 `--last`。

依据官方文档，Goal 模式提供持续目标推进，但不扩大既有权限；CLI 支持 `codex exec` 和指定会话的 resume。本地定时任务仍依赖电脑开机、应用运行及项目文件可用。因此“不间断”的工程目标是“正常情况下自动接续，断电/重启后恢复进度”，不能承诺关机、断网、额度耗尽时仍计算。本次仅制定计划，没有创建 Goal 或定时任务。

待实现的持久化与控制：

- `.local-services/experiments/state.sqlite`：run_id、experiment_id、question_id、阶段、attempt、租约、心跳、下一步；独立于业务 SQLite。
- 每题结束立即事务提交，状态至少区分 pending / running / succeeded / failed / timeout。评价是否正确单独记录，执行完成不等于答对。
- 同一 `(run_id, question_id)` 有唯一结果键，attempt 单独保存。恢复只接续未完成阶段；只有服务/传输故障进行有界重试，不能因评分错了就额外抽样直到答对。
- 初期串行子进程答题；确需并发时每题独立进程，不在一个 Python 进程内并发切库，因为当前工具含全局数据库和 session 状态。
- 每题拟定上限：最多 40 次 LLM 调用、15 分钟总时长；API 短暂错误最多 3 次带退避重试，官方 SQL 执行超时初始 30 秒。先做冒烟验证，再冻结 B0 与所有候选相同预算。
- 模型服务、PG、嵌入服务暂时失败：健康检查、有限重启/退避并继续；认证失效、持续服务故障、磁盘空间不足：落盘为明确等待状态，仅通知实际阻塞，不空转烧额度。
- 用户已明确不设 API 费用上限，额度耗尽即停止。配置为 `max_api_cost=null`、`max_total_api_calls=null`、`stop_on_insufficient_balance=true`；每题调用/纠错/时间上限仍保留，避免单题失控。禁止自动充值或切换另一付费账号来继续。
- 根据服务返回的错误码和错误内容识别余额/配额耗尽，记录 `stopped_insufficient_balance`，停止派发新题与研究轮次并保存已完成记录；未开始题保持可恢复状态，不虚构为已完成。普通 HTTP 429 限流使用退避，不能一律当成余额不足；不要仅凭 HTTP 状态码猜测。
- 保存实际 token 和费用；服务不提供 token/单价时记录调用数及估算标记。用户提到夜间折扣，后续先核实实际 API 服务商的优惠时区、时段与模型资格，再配置大批次的夜间运行窗口，不在计划中编造折扣比例。未设置明确窗口前，不擅自让可执行工作等到夜间。
- 监督进程使用单实例锁，检测退出码和心跳，回收过期运行租约；在重启子进程前确认原进程已结束，避免重复费用和同时改文件。
- 基线和候选分别绑定 Git commit、数据/提示词/技能/配置哈希、模型标识和向量索引版本；配置变化后建立新 run_id，不能把两版结果拼成一个实验。
- 索引重建使用版本化表或快照；正在评估的版本不可被另一轮覆盖。保留完整 embedding 输入规则和模型文件哈希，便于复现。
- 常态下每批约 10 题写进度，不要求用户回复；只在基线完成、候选提升、采用/拒绝或真实阻塞时通知。用户明确停止时立即保存并停止新工作。

每完成约 5 个主要改进实验形成一次研究周期报告并自动规划下一周期，不以周期边界作为等待用户回复的理由。+2 个百分点且复核方向一致作为首个里程碑，达到后保留最佳版本并继续寻找下一项有证据的方法。用户尚未指定最终目标分数或总轮数，因此研究循环持续到用户停止、API 额度耗尽或不可自行解决的外部阻塞；不能保证一定获得提升。Codex 自身会话/账号额度是独立限制，SQL API 仍有余额不代表 Codex 一定能继续运行，必须将任务状态保存在磁盘。

**7. Git 暂存与实验分支规则**

用户已授权在 `codex/dev_legion` 暂存推送，以及为重构方法发布 `dev_{time}` 分支，后续不逐轮重复询问这些操作。

- `codex/dev_legion` 保存可运行实验基础设施与已经采用的改进；另在实验状态中明确 best_commit，暂存提交本身不表示性能最佳。
- 每个需要重构的方法从当前最佳版本建立 `dev_YYYYMMDD_HHMMSS`，例如 `dev_20260921_143000`，时间采用 Asia/Shanghai；同秒冲突追加序号。
- 尽量使用独立 worktree 进行候选实现，避免切分支覆盖用户当前工作。worktree 的运行配置使用同一份本地配置路径，不复制密钥进 Git。
- 方法说明 → 实现与必要测试 → 提交 → push 新分支 → 运行比较/更新实验摘要。推送新分支用于暂存，不必等证明提升；采用到主工作分支则依据第 5 节。
- 失败分支也可推送并标注 rejected，保留可复现实验；最佳版本继续作为下一轮出发点。Git 只做正常追加提交，不以 force push 覆盖历史。
- 推送前检查 diff 与待提交文件；仅发布代码、无密钥的配置模板、题目 ID 清单、方法和指标摘要。API 密钥、数据库口令、完整数据库、模型权重和原始大日志留在本地忽略目录。
- 推送认证临时失败时保留本地 commit、标记待同步；不因此丢失实验，也不宣称已经推送成功。
- 当前未提交的既有变更需要先审阅、记录来源并建立基线 commit，不能让候选绕过这些改动直接从旧 HEAD 开始。

**8. 要补的文件与每轮交付**

以下是待实现入口，尚不可直接运行：

| 待实现文件 | 职责 |
|---|---|
| `experiments/prepare_dataset.py` | 校验 manifest、导出隔离的生成/评分输入、检查数据版本 |
| `experiments/run_batch.py` | 调用现有 AgentService，进程隔离、最终 SQL 提取、断点续跑 |
| `experiments/evaluate.py` | 适配锁定的官方评价器，保存逐题 EX 和错误诊断 |
| `experiments/compare.py` | 配对比较、分组统计、重复试验与成本统计 |
| `experiments/supervisor.py` | 状态机、预算、服务恢复、触发下一阶段和单实例控制 |
| `experiments/experiment.example.json` | 无密钥的模型引用、并发/超时/预算、检索与实验配置 |
| `deploy/build-column-index.py` | 可重复的 SQLite 全量双向量入库与索引版本管理 |

每轮本地目录：`.local-services/experiments/runs/<run_id>/`。至少保存：

```text
run_manifest.json        # commit、版本/哈希、参数、模型、时间、预算
predictions.jsonl        # 300 条题目 ID、question、最终 SQL、提交来源、终态
predictions_official.json
gold.sql + difficulty.jsonl  # 仅评价器使用
scores.jsonl            # 逐题官方 EX、错误/超时、执行统计
traces/                 # 召回、M-Schema、工具、纠错、模型调用轨迹
comparison.json         # 原错变对/原对变错、分组指标、成本
diagnosis.md            # 已查证原因与待证假设
paper_notes.md          # 论文原文链接、相关方法、适用前提、取舍
next_action.json        # 下一步与恢复所需状态
```

Git 中仅保存轻量实验说明与汇总；本地完整结果不因换分支丢失。最终交付至少有 B0 和最佳版本各 300 条完整记录、官方 EX 对比、采用/拒绝方法列表、代码 commit/远端分支及精确复现命令。

**9. 接下来可直接推进的顺序**

1. 基于已固定的 300 题 manifest，先完成数据导出与评分自检，以及全量 798 列索引；这些工作无需 SQL 生成 API。
2. 补齐技能与批量运行/恢复机制；使用已配置的 `.env` / `deepseek-v4.1-flash`，验证接口连通性、工具调用能力并做冒烟。
3. 冻结 B0、跑完首轮 300 题，保存 EX 和错误分布；再决定第一项论文/方法实验。
4. 配置 Goal/监督运行与 Git 推送校验，按状态机自主迭代，不在每轮实验后等待“继续”。

模型路径、模型名称和费用规则已明确，无需重复询问。接下来主要是实现运行器、补齐全量索引和主流程并验证接口。启动常驻运行需要电脑持续开机、不自动休眠、网络和凭证可用；不需要另建 PostgreSQL 业务库。

可在准备好后用于启动 Goal 的目标文本：

> 按 AUTONOMOUS_MINIDEV_PLAN.md 和固定的 mini_dev_300_manifest.json 完成全量索引、批量预测、官方 EX 基线和自主改进循环。使用 F:/data/VSCodeproject/AIDB-SQL/.env 中的 deepseek-v4.1-flash。保持 SQLite 为唯一业务来源，PG 只存元数据/向量。先建立可恢复运行器，每轮单一主要假设，基于失败证据查原始论文再实现。不设 API 总费用和总调用次数上限，保留单题重试/时长限制；API 额度耗尽即保存状态并停止，不自动充值。授权推送 codex/dev_legion 及 dev_YYYYMMDD_HHMMSS 方法分支，保存所有实验和最佳版本。正常阶段自动继续，周期报告后接续下一周期；遇实际外部阻塞保存状态并说明。获得提升要复核，未提升则继续归因和研究；不修改评分口径、不删除失败题、不泄漏标准答案，直到用户停止或真实资源边界。

参考：

- [OpenAI Docs：长期目标与完成条件](https://learn.chatgpt.com/docs/long-running-work)
- [OpenAI Docs：Codex 非交互执行与会话恢复](https://learn.chatgpt.com/docs/non-interactive-mode)
- [OpenAI Docs：本地定时任务运行条件](https://learn.chatgpt.com/docs/automations?surface=app)
- [本地官方 EX 评价器](F:/data/VSCodeproject/mini_dev/evaluation/evaluation_ex.py)
- [本地官方评分输入与数据库连接实现](F:/data/VSCodeproject/mini_dev/evaluation/evaluation_utils.py)
